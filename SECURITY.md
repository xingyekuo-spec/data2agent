# 安全政策 / Security Policy

本项目的代码会以只读方式直连工厂 ERP 数据库,我们把安全问题视为最高优先级。

## 报告漏洞

- 请**不要**在公开 issue 中披露安全漏洞;
- 发送邮件至:security@data2agent.dev(占位,public 前替换为真实邮箱);
- 我们承诺 72 小时内响应,修复发布前不公开细节(负责任披露)。

## 设计层安全承诺

- 对源数据库**绝对只读**:不写表、不建触发器、不建临时表;
- 连接凭证本机加密存储,不进日志、不出网;
- 查询限时(默认 30s)、限行数、可配错峰窗口;
- 所有出网调用(LLM / SaaS API)走显式白名单。
