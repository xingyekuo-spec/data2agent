# data2agent v0.3 开发执行索引

> 状态：进行中 r14（M5 已关闭；M6 待开始）
> 日期：2026-07-22
> 当前里程碑：M6 一键验收与 v0.3 发布（待开始）
> 人力口径：1–2 名开发人员，预计 3–4 周；以门禁通过为准，不按自然周强行切换
> 开始基线：`v0.2.0` / `88b5e23`
> 上层基线：[产品开发路线图 §6](2026-07-17-product-development-roadmap.md)
> 设计基线：[元模型 §6](../../design/01-metamodel.md) · [抽取框架 §7](../../design/02-extraction.md) · [MCP 网关 §3.1](../../design/03-mcp-gateway.md) · [控制台 §10](../../design/05-console.md)

## 1. 版本目标

v0.2 回答“发生了什么”，v0.3 回答“为什么得到这个结果”。发布门槛是任意关键结果可以追溯到输入、映射规则、已发布数据集版本和查询时的结果摘要。

## 2. 执行规则

1. 严格按 `M1 → M2 → M3 → M4 → M5 → M6` 推进；未通过当前里程碑退出门禁，不切换下一里程碑。
2. 版本、preview、血缘和 evidence 必须消费同一元模型，不建立旁路规则副本。
3. preview 不写正式对象表、隔离区、水位或当前数据集版本。
4. MCP 只能读取 `published` 数据集；构建失败或熔断必须继续服务上一稳定版本。
5. 血缘和 evidence 的敏感值遵循现有出口脱敏规则，不能成为解敏旁路。
6. 每项任务按“契约/设计 → 实现 → 自动测试 → 页面验收 → 证据归档”闭环。
7. v0.4 的批次回执、E6b、生产 TLS、多主体凭据轮换和试点压测不提前进入本队列。

## 3. 里程碑队列

| ID | 状态 | 目标 | 关键交付物 | 退出门禁 | 详细任务文档 |
| --- | --- | --- | --- | --- | --- |
| M1 | `已完成` | 建立版本身份和持久化契约 | `template_version`、确定性 `binding_hash`、dataset/object version 元数据与迁移、只读契约 | 摘要稳定且语义变化可检测；旧库可幂等升级；现有 apply/MCP 行为不回归 | [M1 版本身份与数据集元数据](2026-07-21-v0.3-m1-version-metadata.md) |
| M2 | `已完成` | 对象层按数据集原子发布 | 影子构建、全体校验、短事务切换、上一稳定版本、publish/rollback API | 无读空窗；任一关键对象失败时全体不发布；可回滚上一稳定版本 | [M2 对象层按数据集原子发布](2026-07-21-v0.3-m2-atomic-dataset-publish.md) |
| M3 | `已完成` | 映射 Preview | raw 样本/临时 binding、成功与隔离预览、diff、枚举与规则覆盖率 | Preview 与正式 apply 复用转换核心且无正式副作用 | [M3 映射 Preview](2026-07-21-v0.3-m3-mapping-preview.md) |
| M4 | `已完成` | 字段级血缘 | apply 产出、lineage API、对象字段三次点击追溯视图 | 关键字段可追溯到 raw 记录、规则和批次；敏感源值不泄漏 | [M4 字段级血缘](2026-07-22-v0.3-m4-field-lineage.md) |
| M5 | `已完成` | MCP 会话证据 | principal/session/query、不可预测 ID、结果摘要与摘要校验、持久审计 | 跨主体/会话引用被拒绝；建议卡固化查询时摘要和警示 | [M5 MCP 会话证据](2026-07-22-v0.3-m5-mcp-session-evidence.md) |
| M6 | `待开始` | 一键验收与 v0.3 发布 | validation Run、页面报告/JSON、全链路证据链接、分发验收 | 路线图 §6.7 全部门禁通过，接单评审关键数字可追溯 | [M6 一键验收与发布](2026-07-22-v0.3-m6-validation-release.md) |

## 4. 依赖关系

```text
M1 版本身份/元数据
  └─ M2 原子发布与 dataset_version
       ├─ M3 preview（复用正式转换核心）✓
       ├─ M4 lineage（绑定已发布 dataset_version）✓
       └─ M5 evidence（记录查询实际 dataset_version）✓
            └─ M6 一键验收与发布 ← 当前
```

M3 与 M4 都依赖 M2 的稳定版本语义；M5 依赖 M2 提供查询实际读取的数据集版本。为保持单一主线，不并行打开多个里程碑。

## 5. v0.3 发布总门禁

- 关键对象字段具备字段级血缘；
- 映射 preview 与正式 apply 隔离；
- 对象层原子发布并记录 dataset/object version；
- MCP evidence 按主体和会话隔离；
- 一键验收覆盖完整演示链；
- 接单评审建议卡中的关键数字能够追溯到查询结果摘要；
- `/`、`/v0`、`/v1`、现有 CLI 和便携包能力无回归；
- 后端、前端、OpenAPI、分发和端到端测试全部通过。

## 6. 当前执行入口

```text
里程碑：M6
状态：待开始
上一里程碑：M5 MCP 会话证据（已完成）
M5 实施分支：codex/v0.3-m5-mcp-session-evidence
M5 开始代码基线：906a0dc
M4 已交付：
  d2a_field_lineage / d2a_field_lineage_input 两表 + 幂等迁移 + freeze/immutability 触发器
  ApplyVersionContext + 候选表与 lineage 同一 BEGIN IMMEDIATE 事务原子写入
  publish/rollback/stale recovery/GC 全生命周期 lineage 收口
  GET /api/objects/{object}/{key}/lineage（published snapshot 只读查询、脱敏、审计）
  前端 ObjectLineageDrawer + lineage store + Mock fixture
  对象表 lineage_refs 与 rows 对齐；三次点击追溯到 raw 记录、规则和批次
M4 最终门禁证据（相对 M3 基线 805/216/196，均无减少）：
  后端 → 917 passed, 4 skipped（净增 112）
  前端 → 222 passed（净增 6）
  E2E → 196 passed, 0 failed（持平）
  OpenAPI / 模板校验 / Smoke / 分发 / diff --check → 全部通过
M5 已交付：
  d2a_gateway_query_evidence / proposal / proposal_evidence / audit 四表、索引与 immutable trigger
  principal + session 上下文、不可预测 query/proposal ID、canonical result digest 与有界摘要
  query/proposal 原子持久化、跨主体/会话/source/version/digest 拒绝与 rejection audit
  query/proposal detail API、Console tab session header、MCP Lab 冻结摘要与 Mock/Real 同型
  FastMCP ServerSession 弱引用隔离、logout/401 evidence 边界清理、M5 E2E 调用链
M5 最终门禁证据（相对 M4 基线 917/222/196，均无减少）：
  后端 → 953 passed, 4 skipped（净增 36）
  前端 → 225 passed（净增 3）
  E2E → 197 passed, 0 failed（净增 1）
  OpenAPI / Smoke / /v1 分发 / diff --check → 全部通过
M6 开始约束：
  复用 M5 的 evidence 事实来源，不建立查询或建议卡旁路
  不提前实现 v0.4 批次回执、RBAC 或压测
M6 详细计划：2026-07-22-v0.3-m6-validation-release.md
计划实施分支：codex/v0.3-m6-validation-release（45de9a6 为代码基线）
```
