# data2agent v0.2 开发执行索引

> 状态：执行中 r11
> 日期：2026-07-20
> 当前里程碑：M5 隔离与模板只读验证（待验收）
> 人力口径：1–2 名开发人员，预计 5–6 周；以门禁通过为准，不按自然周强行切换
> 上层基线：[产品开发路线图](2026-07-17-product-development-roadmap.md)  
> 设计基线：[控制台设计](../../design/05-console.md) · [Vue Console 实施规格](../specs/2026-07-15-console-ui-design.md)

## 1. 文档用途

本文件把 v0.2 路线图转换为可逐项执行、可暂停、可验收的任务队列。它只管理里程碑顺序、依赖、交付物、状态和证据，不重复详细实现步骤。

每个里程碑开始前建立独立任务文档；执行中只允许一个里程碑处于 `进行中`。当前 M4 已关闭，M5 详细任务文档是下一执行入口；M6 任务在隔离与模板只读能力稳定后编写，避免提前固化错误假设。

## 2. 执行规则

1. 严格按 `M1 → M2 → M3 → M4 → M5 → M6` 推进；未通过退出门禁，不切换下一里程碑。
2. 每项任务按“契约或设计 → 实现 → 自动测试 → 页面验收 → 证据归档”闭环。
3. 现有 Jinja2 管理页、`/v0` 和管理 API 在迁移期间保持可用；不以 Vue 开发为由破坏应急入口。
4. Mock、Demo、Real 共用领域类型和组件；任何非真实数据必须持续展示环境标识。
5. `unknown`、请求失败和未执行不得转换为绿色、零数据或正常空列表。
6. 涉及在线映射编辑、生产 binding 发布、ERP 写回、多租户和 PostgreSQL 全量迁移的需求不进入 v0.2。
7. 映射编辑、E6b 增强和 PostgreSQL 改造不得与本队列并行抢占 1–2 人的关键路径。

## 3. 状态定义

| 状态 | 含义 |
| --- | --- |
| `待开始` | 前置条件尚未满足或尚未进入实施 |
| `进行中` | 当前唯一的主开发里程碑 |
| `待验收` | 实现完成，正在收集或复核门禁证据 |
| `已完成` | 退出门禁全部通过并记录证据 |
| `阻塞` | 存在无法由当前任务内部解决的外部依赖 |

“代码已合并”不等于“已完成”；没有自动测试、页面验收或约定的运行证据时，状态最多为 `待验收`。

## 4. v0.2 里程碑队列

| ID | 状态 | 目标 | 关键交付物 | 退出门禁 | 详细任务文档 |
| --- | --- | --- | --- | --- | --- |
| M1 | `已完成` | 固定前后端契约基线 | 全部现有管理 API 的 request/response model、OpenAPI 快照、漂移 CI、契约测试 | 核心响应无 `unknown`；快照漂移可阻断 CI；Jinja2/`v0` 行为兼容 | [M1 API 契约基线](2026-07-18-v0.2-m1-api-contract-baseline.md) |
| M2 | `已完成` | 建立 Vue 工程与可独立演示的 Mock 骨架 | Vue 3/Vite/TS、路由与布局、生成类型、Mock/fixture、状态组件、前端 CI | 后端不启动也能演示全部规定场景；类型、构建和组件测试通过 | [M2 Vue 工程骨架与 Mock](2026-07-18-v0.2-m2-vue-skeleton-and-mock.md) |
| M3 | `已完成` | 交付总览与管道状态 | Dashboard、服务健康、管道图、告警、最近运行、版本与陈旧状态 | 30 秒判断链路健康；失败可定位到节点；不可检测显示 `unknown` | [M3 产品总览与管道状态](2026-07-18-v0.2-m3-overview-and-pipeline.md) |
| M4 | `已完成` | 交付运行、审计和数据只读浏览 | Run 列表/详情、SQL/访问审计、raw/对象数据浏览、筛选、分页、限流 | 能说明表、行数和水位变化；浏览走服务端分页；不解析日志判断状态 | [M4 运行、审计与数据只读浏览](2026-07-18-v0.2-m4-runs-audit-data.md) |
| M5 | `待验收` | 交付隔离与模板只读验证 | 隔离分组/详情/阈值、旧版本状态、模板与 binding/规则只读展示 | 能解释失败原因与熔断影响；不能从页面直接编辑或发布 mapping | [M5 隔离与模板只读验证](2026-07-20-v0.2-m5-quarantine-and-templates.md) |
| M6 | `待开始` | 交付 MCP Lab 并完成 v0.2 发布验收 | 工具白名单、参数表单、结构化结果、原始 JSON、耗时与错误展示、便携包验证 | 8 个主页面可用；Settings 只读；Mock/Real 可辨；`/v1` 分发门禁通过 | M5 通过后生成 |

## 5. 固定的产品与路由边界

### 5.1 页面边界

v0.2 发布必须具备以下 8 个主页面：

1. 总览；
2. 管道；
3. 运行；
4. 审计；
5. 数据；
6. 隔离；
7. 模板；
8. MCP Lab。

Settings 是只读辅助页，读取 `GET /api/config`，不计入 8 个主页面，也不承诺在线编辑生产配置。

### 5.2 挂载与兼容边界

- Vue Console 固定挂载于 `/v1`。
- `/` Jinja2 管理页和 `/v0` 简版页继续保留。
- 便携包缺少 Vue `dist` 时，`/v1` 必须明确提示“控制台未安装或未构建”，不得静默回落到其他页面。
- `/ingest/reconcile` 属于数据推送协议面；`/api/**` 属于控制台管理面，二者不因前端建设而合并。
- Token 只允许由前端保存在当前标签页的 `sessionStorage`，不得落入 `localStorage`、URL、日志或错误上报正文。

## 6. 跨里程碑完成定义

每个里程碑关闭前必须同时满足：

- 实现与当前任务文档的范围一致，没有顺带开放非目标写操作；
- 自动测试覆盖正常、空数据、失败、无权限以及适用的边界条件；
- 接口、生成类型和 fixture 没有未解释的漂移；
- 页面能区分 `MOCK / DEMO / REAL`，并正确表达 `unknown`；
- 相关文档和验收命令已更新；
- 没有新增明文 Token、敏感业务字段或无限制数据返回；
- `git diff --check` 通过，现有后端测试未回归。

## 7. v0.2 发布总门禁

M6 完成后按以下清单统一验收；任一必选项失败，版本不得标记为 v0.2 完成。

| 类别 | 必选验收 |
| --- | --- |
| 页面 | 8 个主页面可访问；Settings 只读可用；正常、空、运行、警告、失败和不可达状态均可呈现 |
| 真实性 | 页面持续展示 `MOCK / DEMO / REAL`；未知状态不变绿；错误不伪装为空数据 |
| 契约 | 后端 OpenAPI、仓库快照和 TypeScript 类型一致；漂移 CI 有失败用例 |
| 可观察性 | 不查看 SQLite 或自由文本日志即可判断链路状态并定位节点失败 |
| 安全 | 浏览接口有分页和上限；敏感字段有标识或脱敏；Token 不持久化到跨会话存储 |
| 兼容 | `/`、`/v0` 和既有管理能力仍可用；动作语义未被 GET 或页面加载隐式触发 |
| 分发 | 开发构建和便携包均验证 `/v1`；缺少 `dist` 时给出明确诊断 |

## 8. 任务状态与证据记录

每次推进只更新以下内容：

```text
里程碑：M<n>
状态：待开始 / 进行中 / 待验收 / 已完成 / 阻塞
完成项：<任务 ID 与简述>
验证：<命令、测试结果或页面证据>
遗留：<明确留给下一任务的内容>
决策：<产生契约或范围变化时填写>
```

证据优先记录为可重复运行的测试和命令。截图只用于视觉验收，不能替代接口、权限、错误或数据正确性测试。

## 9. 当前执行入口

M4 已通过自动门禁、多轮代码复审和最终审核并关闭，实现提交为 `b38d722`。当前下一执行入口为 [M5 实施计划](2026-07-20-v0.2-m5-quarantine-and-templates.md)，先执行 M5-T01 契约与只读/对象级 retry 边界，不提前实现 M6 MCP Lab。M1 最终门禁修复提交为 `93ae44d`；M2 最终基线提交为 `56648e3`；M3 实现提交为 `0b8e614`、关闭提交为 `571d764`。

```text
里程碑：M1
状态：已完成
完成项：
  M1-T01 路由与 wire shape 契约测试
  M1-T02 contracts.py 共享模型
  M1-T03/T04 17 个 API 的 response_model
  M1-T05 HttpError/Bearer OpenAPI 安全声明
  M1-T06 console-ui/openapi.json + export 脚本
  M1-T07 CI --check 漂移门禁
  M1-T08 全量回归与文档
验证：
  .venv/bin/python -m pytest tests -q          # 168 passed, 4 skipped
  .venv/bin/python -m pytest tests/test_console.py tests/test_console_contract.py -q
                                                 # 26 passed
  python scripts/export_console_openapi.py --check console-ui/openapi.json
  python scripts/smoke_admin_ui.py             # SMOKE PASSED
  python -m data2agent.metamodel.validate templates
  git diff --check
交付 M2：
  - console-ui/openapi.json 作为 openapi-typescript 输入
  - 尚未创建 Vue 工程 / package.json
  - v0.2 新业务端点(pipeline/runs/{id}/data/objects/templates)未建,属后续里程碑
  - legacy 时间字段尚未迁移为带时区 datetime
决策：M1 保持数组列表最外层 wire shape,不做分页 envelope 破坏性变更
```

```text
里程碑：M2
状态：已完成
完成项：
  M2-T01 7 个 v0.2 契约桩(pipeline/runs{id}/data/objects/templates/proposals),
        具名 schema + 运行时 501,不含伪造成功;SetupResponse 联合去掉
        discriminator,修复 openapi-typescript 把 ok 重写为字符串枚举的问题
  M2-T02 console-ui Vue 3/Vite 8/TS 工程,Element Plus/Pinia/Router,
        base=/v1/,dev 代理 /api→:8849,npm ci 可重复安装
  M2-T03 Node API 生成链(generate-api-types.mjs):注入 JsonValueInput/Output
        手写别名解决 TS2502,anyOf 形状校验防漂移;api:check 漂移证明有效
  M2-T04 模式解析(dev 默认 mock,prod 强制 real,mock 永不进生产构建)、
        唯一 openapi-fetch client(sessionStorage Bearer、401 清 Token 回调)、
        call() 统一错误包装(失败不变空数据)
  M2-T05 MSW(browser+node)+ 10 个 satisfies typed fixture 场景,
        token-invalid/unknown-error 传输级短路,未匹配请求直接报错
  M2-T06 AppLayout/TopBar/SideMenu/AuthDialog + 9 路由(history,标题/激活一致)
  M2-T07 state.ts 请求/领域判别联合 + StatusBadge/Loading/Empty/Error/
        Unknown/FeaturePlaceholder(unknown 为灰,非绿)
  M2-T08 9 视图骨架 + overview/settings 两个 store 切片 + pipeline shell
        (Mock 渲染节点,Real 501 显示尚未接入)
  M2-T09 Vitest 68 个测试;ci.yml frontend job(npm ci→api:check→lint→
        typecheck→test→build→check-dist)
  M2-T10 dev/preview 冒烟、console-ui/README.md、本记录
验证：
  cd console-ui && npm ci && npm run api:check   # OK:类型与 openapi.json 一致
  npm run lint && npm run typecheck              # 通过(无显式 any 逃逸)
  npm run test                                   # 68 passed(12 files)
  npm run build && node scripts/check-dist.mjs   # OK:base=/v1/ 无 CDN 全量产物无Mock
  npm run dev -- --port 5173                     # /v1/ → 200,mockServiceWorker → 200
  npm run preview -- --port 4173                 # /v1/ → 200,assets → 200
  .venv/bin/python -m pytest tests -q            # 172 passed, 4 skipped
  .venv/bin/python -m pytest tests/test_console.py tests/test_console_contract.py -q
                                                 # 30 passed(含 4 个契约桩测试)
  python scripts/export_console_openapi.py --check console-ui/openapi.json
  python scripts/smoke_admin_ui.py               # SMOKE PASSED
  python -m data2agent.metamodel.validate templates
  git diff --check
审核结论(2026-07-18 已通过):
  - 后端关闭,Mock 模式切换 10 个场景,确认顶栏 MOCK 常显、
    token-invalid 弹认证提示、unknown-error 显示可重试错误、
    empty-install 为空态而非绿色
  - dev:real 连本机 :8849,总览/设置渲染真实数据,管道页显示 501 尚未接入
遗留（M3 真实 API 缺口）：
  - GET /api/pipeline 真实实现:节点状态聚合(unknown 口径、最近成功/失败、
    输入输出行数、耗时、错误摘要、版本),数据源为落地库 + 服务探测
  - 总览 Dashboard 的告警与"当前数据是否旧版本"口径(M3 定义;版本字段
    当前恒 null,待 v0.3 dataset/object version)
  - 运行/审计/数据/隔离/模板/MCP Lab 端点仍为 501 契约桩,随 M4–M6 实现
  - legacy 时间字段(runs/audit/quarantine)仍是本地无时区文本;v0.2 新端点
    已按带时区 datetime 建模,迁移口径在 M4 统一
决策：
  - 生成器对自递归 $ref 的内联展开触发 TS2502,改用注入别名方案
    (generate-api-types.mjs 校验 anyOf 形状,漂移即失败)
  - openapi-typescript defaultNonNullable 默认行为使带默认值字段在 TS 必填,
    请求体显式传全字段(如 deep:false),保持请求/响应双向类型诚实
  - vitest node 环境 import.meta.env.BASE_URL 恒为 '/',生产 base 证据由
    scripts/check-dist.mjs 在构建后检查产物
复审修复(评审发现 5 项,已全部修复并补回归测试):
  - P1 场景切换不刷新页面:scenario-epoch.ts 代际信号 + router-view key
    重挂载;AppLayout 测试覆盖 healthy→unknown-error→healthy 往返
  - P1 生产构建携带 MSW/fixture:vite build 别名 ScenarioSwitcher→Stub、
    mocks/browser→disabled、publicDir 关闭;check-dist.mjs 改为扫描 dist
    全部文件的 Mock 标记(mockServiceWorker/场景 ID/fixture 数据)
  - P2 MCP Lab 路径与规格固定值对齐:/mcp-lab→/mcp
  - P2 ErrorState 对 501 显示「尚未接入」(独立 testid 与样式),不再是
    通用「请求失败」;PipelineView 补 Real-501 渲染测试
  - P2 布局按最终参考 UI 对齐规格 §3:白色顶栏、240px 品牌侧栏、可折叠
    两级分组菜单(运维监控/数据管理/Agent/系统)、浅蓝圆角胶囊选中态、
    路由页面标题与紧凑卡片内容区
  - 复审修复引入的回归:strictUnhandledRequest 对全部未匹配请求抛错,
    把 Vite dev 模块请求(/v1/src/**)也打断,导致动态 import 500、路由
    无法挂载、页面空壳不响应场景切换;已改为仅 /api/* 抛错、非 API 放行,
    并补 handlers 分流测试与 App 全链路用户操作测试(src/App.test.ts)
```

```text
里程碑：M3
状态：已完成（2026-07-18 审核通过；实现提交 `0b8e614`）
完成项：
  M3-T01 overview 增加 summary/versions/binding/alerts/recent_runs/trend/
        notes 具名块,pipeline 增加 overall_status 与节点扩展字段
        (status_reason/observed_at/run_id/source/detail_path);旧字段兼容
  M3-T02 d2a_sync_run 幂等迁移增加 run_type(PRAGMA 检测后 ALTER),
        start_run 强制类型;sync/apply/reconcile 调用点与 3 个迁移测试
  M3-T03 observability.py:带时区时间转换、新鲜度阈值(max(2×sync_every,
        300s))、safe 错误摘要、状态折叠、七节点状态计算;37 个原语+矩阵测试
  M3-T04 真实 /api/overview:真实计数(失败 null+告警不伪装 0)、告警聚合
        (critical>warning>info)、24h 趋势、版本(app/template 真实,dataset/
        object 恒 null)、只读无副作用;4 个集成测试
  M3-T05 真实 /api/pipeline:固定 7 节点+overall 折叠,local/http sink 分流,
        服务健康≠数据健康,apply 熔断双节点可定位;5 个集成测试
  M3-T06 生成类型与 10 个 fixture 场景同步(status_reason/告警/overall)
  M3-T07 overview/pipeline 双 store 单一所有者+createPoller(5s 固定间隔、
        防重入、隐藏暂停、可见即刷、失败退避至 60s);刷新失败保留旧数据+
        refreshError 标记;TopBar 只读 store(整体状态/隔离数/更新时间)
  M3-T08 Dashboard:4 摘要卡、状态摘要、ECharts 按需趋势、对象分布、
        最近运行、告警、版本与治理、口径说明(主包 959KB,ECharts 按需)
  M3-T09 管道页:桌面横向/小屏纵向 7 节点流程、点击详情面板(键盘/ESC 可关)、
        detail_path 未启用不死链、局部失败不整页转错误屏
  M3-T10 Playwright 浏览器验收 25 项断言(Mock 16 + Real 9),CI frontend
        job 增加 Python+chromium+test:e2e
验证：
  .venv/bin/python -m pytest tests -q            # 240 passed, 4 skipped
  .venv/bin/python -m pytest tests/test_console_observability.py -q  # 52 passed
  .venv/bin/python -m pytest tests/test_console_overview.py tests/test_console_pipeline.py -q
                                                 # 10 passed
  python scripts/export_console_openapi.py --check console-ui/openapi.json
  python scripts/smoke_admin_ui.py               # SMOKE PASSED
  python -m data2agent.metamodel.validate templates
  cd console-ui && npm run api:check             # OK:类型与 openapi.json 一致
  npm run lint && npm run typecheck              # 通过
  npm run test                                   # 88 passed(15 files)
  npm run build && node scripts/check-dist.mjs   # OK:base=/v1/ 无 CDN 全量产物无Mock
  npm run test:e2e                               # 25 项断言全过(Mock+Real)
  git diff --check
开始基线：
  M2 提交 56648e3
  npm test                                      # 68 passed(12 files)
  .venv/bin/python -m pytest tests -q           # 172 passed,4 skipped
关键决策：
  - 状态只由结构化运行、audit 元数据、框架元数据列、配置和服务探测计算，
    不解析自由文本日志、SQL 或 detail 关键词
  - 无法检测、跨机器证据缺失、计数查询失败和未实现版本均保持 unknown/null
  - M3 只增加 run_type，不提前实现 M4 的 Run steps 和数据浏览
  - dataset/object version 在 v0.3 前保持 null；旧对象结果用时间差和最近
    apply 结构化状态表达，不伪造版本号
  - AppLayout 是轮询唯一所有者，默认 5 秒、禁止重入、隐藏暂停、失败退避
遗留（交 M4）：
  - run_type 与历史 NULL 语义就绪;M4 在此基础上统一 Run+steps 模型与
    运行详情(detail_path 契约已预留,当前恒 null)
  - 带时区转换/新鲜度/状态计算工具可复用;legacy 时间字段迁移口径在 M4 统一
  - 运行/审计/数据/隔离/模板/MCP Lab 端点仍为 501 契约桩,随 M4–M6 实现
  - 告警为聚合结果非持久实体;确认/静音/通知与历史表不在 v0.2
复审修复(评审发现 7 项,已全部修复并补回归测试):
  - P1 SQLite 查询异常曾被解释为 idle:事实查询改为抛出异常,节点级
    try/except 降级为 unknown;"no such table" 才算未物化
  - P1 latest_run/ingest 批次未按 source 过滤,跨源记录会混用;
    已补 source 条件与多源回归测试
  - P1 最近成功/失败时间从单条 latest 推导(失败吞掉成功、running 当失败);
    改为 ok/failed/running 分别查询
  - P1 objects=failed 时 MCP 曾落入默认 healthy;现在映射为 failed
  - P2 overview 部分源 raw 失败会返回部分合计冒充总数;任一源失败即
    raw_rows=null,且失败按字段独立归因(不与其他字段互相污染)
  - P2 管道详情面板持有旧对象快照;改为 selectedId + 从当前数据推导,
    轮询后详情同步更新
  - P2 节点 duration_ms 由 started/finished 计算(running 用当前时间),
    mapping 填模板版本、mcp 填组件版本
复审修复第二轮(评审再发现 7 项,已全部修复并补回归测试):
  - P1 崩溃遗留的旧 running 曾永久覆盖更新的成功并产生数年耗时;
    running 仅在是最新一条 run 时有效,孤儿 running 不参与状态与展示
  - P1 错峰窗口暂停(status=paused)曾被当 failed 并计入失败历史;
    改映射 warning 且不进 last_failure_at
  - P1 缺少 sync_every 配置时成功曾被直接判 healthy(2020 年记录也绿);
    erp/extract/raw 在阈值缺失时判 unknown,mapping 的 apply 不受此约束
  - P1 raw/apply 不可检测时 objects 曾自称"与 raw 同步"、MCP 降为 idle;
    objects 传播 unknown,MCP 同步传播 unknown
  - P2 /api/services 每 5 秒请求但无页面消费;Dashboard 状态摘要新增
    console/apply/ingest/mcp 服务徽标,首次加载失败显示"服务状态查询失败"
  - P2 recent_runs/sync_trend 查询失败曾返回 [] 冒充"从未运行/无趋势";
    契约改为可空(null=不可检测),前端与摘要卡同步显示不可检测
复审修复第三轮(评审复审 3 个组合误判 + 1 文档矛盾,已全部修复):
  - P1 上游 unknown 传播不完整(raw 缺抽取时间/缺阈值/legacy 无 run_type
    时 objects/mcp 仍绿);改为按上游节点领域状态传播:raw 或 mapping 为
    unknown 时 objects 必为 unknown,MCP 同步传播
  - P1 apply 的 paused/running 曾被 objects 解释为"apply 失败";
    改为按真实状态分语义:running=进行中、paused=部分更新,只有真正
    失败才算"apply 失败"
  - P1 http 推送在 sync_every 不可解析时旧批次变绿;阈值缺失时 push
    判 unknown,与 erp/extract/raw 一致
  - P3 索引"已提交待验收"与工作树矛盾;改为"实现完毕待验收,尚未提交",
    提交后回填提交号
  - 组合回归测试 +6(raw 缺时间/缺阈值/legacy run_type 传播、apply
    paused/running 语义、push 阈值缺失)
关闭证据(2026-07-18):
  - 关闭前把本记录测试数更新为最终值(已完成:后端 240、前端 88、e2e 25)
  - 勾选 M3 计划 §11 退出门禁并记录证据
  - 实现提交:`0b8e614` (`feat(console): 完成 v0.2 M3 总览与管道状态`)
  - 人工复审通过；M3 已关闭，M4 实施计划已生成
```

```text
里程碑：M4
状态：已完成（2026-07-20 审核通过；实现提交 `b38d722`）
计划任务：
  M4-T01 冻结 Run/step、分页、浏览安全、脱敏与错误契约
  M4-T02 新增 run step 与控制台访问审计幂等存储
  M4-T03 为 sync/apply/reconcile/ingest 写统一 Run steps
  M4-T04 实现运行列表兼容分页与运行详情
  M4-T05 实现 SQL 审计筛选和数据访问审计
  M4-T06 建立白名单、稳定排序、搜索、脱敏与截断浏览内核
  M4-T07 实现 raw/object 目录和分页浏览 API
  M4-T08 同步 OpenAPI、生成类型、service 与 Mock fixture
  M4-T09 实现运行页
  M4-T10 实现审计页
  M4-T11 实现数据页
  M4-T12 完成 Real/Mock E2E、全量回归和里程碑关闭
开始基线：
  M3 实现提交 0b8e614
  M3 关闭提交 571d764
  .venv/bin/python -m pytest tests -q           # 240 passed, 4 skipped
  cd console-ui && npm run test                 # 88 passed(15 files)
  npm run test:e2e                              # 25 项断言通过
关键决策：
  - 旧 Run 无 step 证据时显式 legacy_unavailable，不猜测回填
  - /api/runs 与 /api/audit 保持数组正文兼容，以 X-Total-Count 提供总数
  - Run 查询统一使用 type 参数；数据库 run_type 仅为内部列名
  - raw/object 浏览只接受后端目录/元模型白名单、服务端稳定分页和声明业务键
  - 对象与 raw 已知敏感字段服务端脱敏；未知 raw 分类持续警告
  - Real raw 浏览要求有效管理 Token，允许/拒绝均写不泄密访问审计
  - M4 Vue 仅交付只读能力，不新增 sync/apply/reconcile/retry 按钮
执行入口：
  docs/superpowers/plans/2026-07-18-v0.2-m4-runs-audit-data.md
里程碑：M4
状态：已完成（2026-07-20 审核通过；实现提交 `b38d722`）
完成项：
  M4-T01 RunSummary/RunStep/RunDetail(steps_state)、ColumnMeta/FieldTruncation、
        RawTableCatalog、AccessAuditPage 契约冻结;X-Total-Count 显式声明
  M4-T02 d2a_run_step / d2a_console_access_audit 幂等迁移(新库/旧库/重复初始化)
  M4-T03 sync 表级 step(水位 JSON)、apply 对象 step(熔断 aborted)、reconcile
        segment step、ingest batch Run(step 关联重试不重复);观测失败语义区分
  M4-T04 /api/runs 筛选+分页+X-Total-Count(数组兼容)、/api/runs/{id} 详情
        (steps_state、404)、管道 detail_path 深链
  M4-T05 /api/audit source/action/时间筛选+总数、/api/audit/access 查询
  M4-T06 data_browser:目录/标识符/业务键(key_map 优先,DDL pk 兜底)/稳定排序/
        JSON-safe(BLOB/64KiB/NaN)/列分类/脱敏内核
  M4-T07 raw catalog/page(强 Token+访问审计+失败关闭)、object catalog/page
        (敏感属性永久脱敏;未物化具名 409)
  M4-T08 生成类型与 Mock 同步(accessAudit/rawCatalog fixture,X-Total-Count)
  M4-T09 运行页(筛选/分页/详情抽屉/step/水位/legacy 提示/安全 JSON)
  M4-T10 审计页(SQL/访问双 tab,SQL 默认折叠,raw-only 范围说明)
  M4-T11 数据页(raw/object 目录,服务端列驱动,分类/脱敏/截断/同源 JSON,
        raw 403 安全指引)
  M4-T12 e2e 扩展(Mock 详情深链/legacy/SQL 折叠/raw 脱敏;Real sync/apply/
        reconcile/ingest 结构化 step、legacy 提示、SQL 筛选、raw 拒绝审计/
        注入防护、raw+object 脱敏和浏览无业务副作用)
验证：
  .venv/bin/python -m pytest tests -q            # 301 passed, 4 skipped
  .venv/bin/python -m pytest tests/test_console_runs.py tests/test_console_audit.py \
    tests/test_console_data_browser.py tests/test_console_browse_api.py -q
                                                 # 39 passed
  .venv/bin/python -m pytest tests/test_run_step.py tests/test_run_steps_write.py -q
                                                 # 19 passed
  python scripts/export_console_openapi.py --check console-ui/openapi.json
  python scripts/smoke_admin_ui.py               # SMOKE PASSED
  python -m data2agent.metamodel.validate templates
  cd console-ui && npm run api:check             # OK:类型与 openapi.json 一致
  npm run lint && npm run typecheck              # 通过
  npm run test                                   # 112 passed(19 files)
  npm run build && node scripts/check-dist.mjs   # OK:base=/v1/ 无 CDN 全量产物无Mock
  npm run test:e2e                               # 50 项断言全过(Mock 24 + Real 26)
  git diff --check
遗留（交 M5/M6/v0.3）：
  - Run+step 详情与浏览组件可复用;M5 隔离/模板页接入已验证的对象目录
  - 访问审计当前仅覆盖 raw 浏览;object 审计接入与页面说明留待后续按需
  - validation 一键验收动作 / dataset_version / object 原子发布属 v0.3
    （validation 类型与筛选已纳入统一 Run 模型）
  - 运行详情 detail_path 已通;quarantine ↔ step 关联属 M5
  - 遗留:M5 隔离处理、M6 MCP Lab、便携包 /v1 分发
提交号：b38d722 (`feat: complete M4 runs audit data browsing`)
关闭证据(2026-07-20):
  - 勾选 M4 计划 §11 退出门禁并记录最终验证数量
  - 人工复审问题已全部修复并重新验证
  - M5 实施计划已生成，下一步只进入隔离与模板只读验证，不提前实现 M6
```

```text
里程碑：M5
状态：待验收（R6 修复中）
完成项：
  M5-T01 冻结隔离、模板、指标和对象级 retry 边界契约
  M5-T02 扩展隔离记录/分组/详情 API，保持数组兼容与分页总数
  M5-T03 固定隔离率、熔断阈值和旧对象服务状态口径
  M5-T04 实现模板对象、属性、binding、derived、枚举和指标只读 API
  M5-T05 规范对象级 retry:只允许对象重映射、强确认、只读模式禁用、写入 Run+step
  M5-T06 同步 OpenAPI、类型、service、store 与 Mock fixture
  M5-T07 实现隔离页
  M5-T08 实现模板页
  M5-T09 完成 Mock/Real E2E、安全与兼容回归
开始基线：
  M4 实现提交 b38d722
  .venv/bin/python -m pytest tests -q            # 301 passed, 4 skipped
  cd console-ui && npm run test                  # 112 passed(19 files)
  npm run test:e2e                               # 50 项断言全过(Mock 24 + Real 26)
验证：
  .venv/bin/python -m pytest tests -q            # 525 passed, 5 skipped
  python scripts/export_console_openapi.py --check console-ui/openapi.json
  python scripts/smoke_admin_ui.py
  cd console-ui && npm run api:check && npm run lint && npm run typecheck && npm run test
  # npm run test:166 passed(21 files); build 通过
  # Real E2E 需 Playwright Chromium + Real 后端;Mock E2E 通过
  # npm run test:e2e
关键决策：
  - 隔离页先解释事实与影响范围，不提供单行修复、删除或自动处理
  - 对象级 retry 不等同单行 retry;若启用，必须经确认并进入统一 Run+step 证据
  - 模板页只读展示，不在线编辑/发布 mapping，不自动把 draft 改 verified
  - 指标 draft 状态与 binding draft 同等可见，不能被总览数字掩盖
复审修复(R4):
  - P1 reason 仍泄露实际异常值:改为字段名正则屏蔽 FIELD=VALUE 模式
  - P1 未使用变量阻断 lint 门禁:删除 hasUnknownObj
  - P1 E2E 绕过 retry 确认与 Runs 证据:补 stale 场景与 retry UI 流
  - P2 成功 apply 丢失 batch 关联:ObjectApplyResult 新增 batch_id 字段并传播
  - P2 M5 交付状态与代码不一致:执行索引与计划 §11 同步
当前入口：
  docs/superpowers/plans/2026-07-20-v0.2-m5-quarantine-and-templates.md
```
