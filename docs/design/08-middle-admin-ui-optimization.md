# 08 · 中间机管理界面优化清单

> 状态：**待实施** · 审核基线：2026-08-07
>
> 适用范围：中间机本地管理界面 `middle_admin`（默认 `:8851`），不包含平台 Vue Console。
> 本文列出完整候选项；其中只有 P0 是生产部署前必须完成的阻断项。

## 1. 目标与结论

当前中间机管理界面已经覆盖首次配置、ERP 测连、元数据扫描、抽取计划、运行记录、
推送记录、错误处理和日志查看，基本业务流程可以闭环。主要缺口不在视觉风格，而在
“无人值守运维”能力：后台已有的真实进程状态、自动重启和对账调度没有完整呈现，
维护备份结果也没有形成可查询状态；部分请求失败还可能被页面误显示成空数据。

优化目标：

1. 运维人员打开首页后，10 秒内能够判断 launcher、connector、maintenance、ERP、平台和最近同步是否健康。
2. 页面不得把认证失败、接口失败、心跳过期或进程停止显示成“无配置”“暂无记录”或正常计划状态。
3. 所有会影响长期运行的配置都能在页面中查看；允许编辑的字段经过服务端校验和 revision 乐观锁。
4. 所有后台动作都有明确的开始、执行中、完成、失败、是否自动重启以及后续处理提示。
5. 中间机断网、平台不可达或无人操作时，页面仍能给出可信的本机状态和恢复指引。

## 2. 优先级定义

| 优先级 | 定义 | 部署要求 |
| --- | --- | --- |
| P0 | 会掩盖故障、导致错误运维判断，或缺少生产必需的长期运行状态 | 首个正式生产部署前完成 |
| P1 | 显著降低排障和日常维护成本，或补齐重要操作能力 | 首次稳定性迭代完成 |
| P2 | 一致性、可访问性、响应式和工程质量优化 | 按版本持续完成 |

## 3. P0：生产部署前必须完成

### 3.1 真实进程健康

| ID | 优化项目 | 前端工作 | 后端/运行时配套 | 验收标准 |
| --- | --- | --- | --- | --- |
| MUI-P0-01 | 展示真实进程状态 | 首页展示 launcher、connector、maintenance 的运行/重启中/熔断/停止状态、PID 和重启次数 | 复用 `/api/status.process_status` | connector 被终止后，页面在 15 秒内显示故障，不再只显示计划状态 |
| MUI-P0-02 | 心跳过期判定 | 显示状态更新时间、距今时长和“状态已过期” | 状态接口保留服务端过期判定，避免只依赖浏览器时钟 | 停止 launcher 后，页面显示“状态过期”，不得显示绿色运行中 |
| MUI-P0-03 | 全局严重故障横幅 | 在所有运维页顶部显示持续告警，并提供日志/运行记录入口 | 汇总进程、最近运行、推送和维护状态 | connector 停止、熔断或连续失败时，从任意页面均可见告警 |
| MUI-P0-04 | 区分配置状态与运行状态 | 把“已配置、窗口内、下次预估”和“进程真实运行”拆成不同区域 | `/api/status.schedule_source` 明确标识推算来源 | connector 停止时，“计划已配置”可以保留，但不得暗示任务会正常执行 |
| MUI-P0-05 | 启动与自启动状态 | 展示“本次是否由受监管 launcher 启动”，提供 Windows 定时任务检查说明 | 增加可选的启动模式/自启动检查结果；无法自动检测时返回 `unknown` | 页面能区分受监管启动、手工启动和未知状态，不对未检测状态宣称成功 |

实现依据：[`middle/admin/status.py`](../../data2agent/middle/admin/status.py)、
[`middle/admin/templates/status.html`](../../data2agent/middle/admin/templates/status.html)、
[`scripts/launch_admin_ui.py`](../../scripts/launch_admin_ui.py)。

### 3.2 维护、备份与磁盘

| ID | 优化项目 | 前端工作 | 后端/运行时配套 | 验收标准 |
| --- | --- | --- | --- | --- |
| MUI-P0-06 | 维护进程状态 | 首页显示 maintenance 是否存活、是否重启或熔断 | 复用进程状态 | maintenance 停止后 15 秒内可见 |
| MUI-P0-07 | 最近备份状态 | 展示最近成功时间、文件名、完整性、大小和距今时长 | maintenance 将每次结果原子写入状态文件，API 脱敏读取 | 成功执行后页面可见；超过约定周期未成功时告警 |
| MUI-P0-08 | 磁盘空间告警 | 展示可用空间和阈值，低于阈值显示严重告警 | maintenance/API 返回磁盘统计和检查时间 | 模拟低空间时页面不得仍显示维护健康 |
| MUI-P0-09 | 清理与保留结果 | 展示历史清理数、孤儿 staging 清理数、备份保留数和最近错误 | 持久化结构化维护结果，不解析文本日志 | 成功、部分失败、完全失败三种状态可区分 |
| MUI-P0-10 | 备份恢复指引 | 提供只读恢复说明和备份目录，不直接在运行中覆盖数据库 | 后端仅返回允许公开的路径摘要 | 页面不提供误触即覆盖的恢复按钮；恢复步骤链接到 runbook |

实现依据：[`middle/maintenance/__main__.py`](../../data2agent/middle/maintenance/__main__.py)。

### 3.3 请求错误与登录状态

| ID | 优化项目 | 前端工作 | 后端配套 | 验收标准 |
| --- | --- | --- | --- | --- |
| MUI-P0-11 | 统一检查 HTTP 状态 | 所有 `fetch`/HTMX 请求先判断状态码，再解析业务数据 | API 继续返回统一错误结构 | 401、403、409、500、超时不得显示成空列表或“无源配置” |
| MUI-P0-12 | Token 失效处理 | 401 时清理无效会话、显示登录页，并在登录后返回原页面 | 可增加轻量认证校验接口，或直接以受保护 API 校验 | 输入错误 Token 后有明确提示，不进入伪正常页面 |
| MUI-P0-13 | 退出与重新登录 | 顶栏提供退出；支持在不清浏览器数据的情况下更换管理密码 | 无额外依赖 | 退出后 session Token 被清除，受保护数据不再显示 |
| MUI-P0-14 | 网络/API 故障状态 | 页面显示“管理 API 不可达/请求超时/最近成功刷新时间” | 统一错误码和建议字段 | 拔网或停止 API 后保留最后成功时间，但旧数据明确标记为过期 |
| MUI-P0-15 | 禁止失败静默降级 | 摘要、告警、状态、日志加载失败均使用错误态和重试按钮 | 无额外依赖 | 任何主要卡片只有加载、成功、空数据、失败四种明确状态 |

现状风险集中在 [`middle/admin/templates/status.html`](../../data2agent/middle/admin/templates/status.html)
和 [`middle/admin/templates/errors.html`](../../data2agent/middle/admin/templates/errors.html) 中未统一检查
`response.ok` 的请求。

### 3.4 配置完整性与重启语义

| ID | 优化项目 | 前端工作 | 后端配套 | 验收标准 |
| --- | --- | --- | --- | --- |
| MUI-P0-16 | L1 对账计划 | 配置页展示和编辑每日 `reconcile_at` | 已在配置模型和白名单中支持 | 保存、刷新和服务重启后值一致；非法时间被拒绝 |
| MUI-P0-17 | 深度对账计划 | 展示 `reconcile_deep_at` 和星期 `reconcile_deep_day_of_week` | 已在配置模型和白名单中支持 | 星期与时间联动校验；清空时间时不得保留星期 |
| MUI-P0-18 | 对账状态摘要 | 首页显示最近/下次 L1 和深度对账、最近结果 | 状态 API 增加真实执行记录或明确标为推算 | 页面明确区分“计划时间”和“已执行结果” |
| MUI-P0-19 | 准确的重启提示 | 根据 `restart_required`、`restart_automatic` 显示自动重启、等待生效或人工重启 | 所有配置写接口统一返回重启语义和结果 | 更新 Token 自动重启时不得再提示手工双击程序 |
| MUI-P0-20 | 配置生效状态 | 显示当前页面 revision、运行进程加载的 revision，以及两者是否一致 | worker/launcher 状态增加已加载配置摘要 | 修改配置后可以判断新配置是否已经被 connector 使用 |
| MUI-P0-21 | 首次部署完成检查 | 首配结束后显示：ERP 测连、平台连通、协议兼容、已选表、自启动、备份进程 | 增加聚合 readiness API 或组合现有接口 | 所有必需项通过前不得显示“部署完成” |

实现依据：[`middle/admin/app.py`](../../data2agent/middle/admin/app.py)、
[`shared/admin/config_edit.py`](../../data2agent/shared/admin/config_edit.py)、
[`middle/admin/templates/config.html`](../../data2agent/middle/admin/templates/config.html)。

## 4. P1：稳定性迭代

### 4.1 状态与排障

| ID | 优化项目 | 说明 |
| --- | --- | --- |
| MUI-P1-01 | 源级健康评分 | 按“进程、配置、ERP、平台、最近同步、对账”分别显示，不合成一个模糊绿灯 |
| MUI-P1-02 | 数据新鲜度/SLA | 每个源展示距最近成功同步时长；超过 `sync_every + 容差` 告警 |
| MUI-P1-03 | 最近成功与最近失败并列 | 避免一次成功记录覆盖之后发生的失败，也避免单次失败掩盖长期健康趋势 |
| MUI-P1-04 | 调度详情 | 显示时区、窗口、首轮时间、间隔、下次推算依据和窗口外原因 |
| MUI-P1-05 | supervisor 详情 | 显示重启时间、熔断原因、冷却剩余时间和最近进程退出码；需要状态协议扩展 |
| MUI-P1-06 | 一键复制诊断摘要 | 复制脱敏的版本、进程、时间、最近错误、配置 revision，便于现场反馈 |
| MUI-P1-07 | 日志上下文跳转 | 从进程、运行、推送错误直接跳转相应服务和时间附近日志 |
| MUI-P1-08 | 本机版本与兼容性 | 展示中间机版本、ingest 协议版本、平台支持版本和兼容结论 |

### 4.2 推送、generation 与对账可观测性

| ID | 优化项目 | 说明 |
| --- | --- | --- |
| MUI-P1-09 | generation 追踪 | 运行详情展示 generation ID、开始/心跳/完成/中止状态和平台确认结果 |
| MUI-P1-10 | generation 屏障错误 | 对 lease 过期、旧 generation 被拒、心跳失败给出独立错误分类和处理建议 |
| MUI-P1-11 | 批次回执状态 | 推送详情展示 batch ID、attempt、receipt、幂等命中和重试次数 |
| MUI-P1-12 | 推送重试过程 | 区分正在退避、可重试失败、不可重试失败以及最终失败 |
| MUI-P1-13 | 对账结果页 | 展示 L1/深度对账的表级差异、修复批次和修复后验证结果 |
| MUI-P1-14 | 受控手工对账 | 增加 L1/深度对账触发入口、二次确认和运行跟踪；需先实现受锁保护的后台 API |
| MUI-P1-15 | 水位详情增强 | 展示原始值类型、最近推进量和长期不推进提示；长值折叠，禁止撑破表格 |

### 4.3 配置与日常操作

| ID | 优化项目 | 说明 |
| --- | --- | --- |
| MUI-P1-16 | HTTPS/TLS 高级配置 | 展示/编辑 CA bundle、是否允许受控内网 HTTP；不得提供“关闭证书校验”选项 |
| MUI-P1-17 | 推送可靠性参数 | 展示/编辑请求超时和重试次数，标明单位、上下限和推荐值；需扩展编辑白名单 |
| MUI-P1-18 | 敏感配置只读状态 | 只显示 Token/DSN/CA 是否已配置及更新时间，不回显内容 |
| MUI-P1-19 | 字段级即时校验 | 校验时间、日期、URL、端口、窗口、批量大小和限速，并保留服务端最终校验 |
| MUI-P1-20 | 配置改动预览 | 保存前显示字段差异；密码和 Token 只显示“将更新”，不进入 diff |
| MUI-P1-21 | revision 冲突处理 | 409 时展示“本地值/服务器新值”，允许刷新或重新应用，不只显示通用错误 |
| MUI-P1-22 | 多数据源适配 | 状态、配置、触发和告警始终带 source；禁止默认首个源导致误操作 |
| MUI-P1-23 | 危险动作确认 | 全量刷新、深度对账、批量重试等动作说明影响范围并二次确认 |
| MUI-P1-24 | 动作防重复提交 | 操作期间禁用按钮，显示进度；响应后恢复；重复点击不得启动多个后台任务 |
| MUI-P1-25 | 动作结果一致化 | 所有动作统一返回运行 ID、状态、是否执行、后续链接和错误建议 |

### 4.4 告警与刷新机制

| ID | 优化项目 | 说明 |
| --- | --- | --- |
| MUI-P1-26 | 服务端错误分类 | 将浏览器正则分类改为服务端稳定 `error_code/category/retryable`，避免中英文文案变化导致误分类 |
| MUI-P1-27 | 进程与维护告警入错误页 | 错误页不仅聚合运行/推送失败，也显示进程、备份、磁盘和协议不兼容告警 |
| MUI-P1-28 | 告警去重和生命周期 | 同一根因聚合首次/最近发生时间、次数、恢复时间；恢复后进入已恢复区域 |
| MUI-P1-29 | 静默范围与审计 | 静默时选择时长和范围；严重进程停止/磁盘不足默认不可永久静默 |
| MUI-P1-30 | 智能轮询 | 页面隐藏时降低频率；连续失败指数退避；恢复后自动刷新并提示 |
| MUI-P1-31 | 最近成功刷新时间 | 每块异步数据均标明最后成功刷新时间，避免用户误读旧数据 |

## 5. P2：体验、可访问性与工程质量

### 5.1 页面体验

| ID | 优化项目 | 说明 |
| --- | --- | --- |
| MUI-P2-01 | 统一四态组件 | 统一加载、失败、空数据、成功状态和重试入口 |
| MUI-P2-02 | 统一通知组件 | 替换零散 `alert()`、原始 JSON 和自由文本结果，统一成功/警告/失败提示 |
| MUI-P2-03 | 统一术语 | 固定使用“抽取、推送、运行、批次、generation、对账、维护”，为 generation 提供帮助说明 |
| MUI-P2-04 | 时间展示一致 | 绝对时间 + 相对时间，明确 `Asia/Shanghai` 或配置时区，避免浏览器时区造成误解 |
| MUI-P2-05 | 数值格式化 | 行数、字节数、时长、速率和磁盘容量使用一致格式，保留可复制原值 |
| MUI-P2-06 | 长内容处理 | 错误、SQL 摘要、水位和路径支持折叠、复制、换行，不破坏卡片宽度 |
| MUI-P2-07 | 导航信息架构 | 保持“运维/配置”分组，增加维护入口或把维护并入状态详情，避免新增孤立页面 |
| MUI-P2-08 | 操作反馈文案 | 明确“已提交”“正在执行”“已生效”“等待重启”，避免全部写成“已保存” |

### 5.2 响应式与可访问性

| ID | 优化项目 | 说明 |
| --- | --- | --- |
| MUI-P2-09 | 小屏布局 | ≤900px 导航和工具栏折行；表格横向滚动；操作按钮保持可触达 |
| MUI-P2-10 | 键盘标签页 | 实现左右键切换、`aria-selected`、`aria-controls` 和正确的 `tabindex` |
| MUI-P2-11 | 焦点可见 | 所有链接、按钮、输入框提供 `:focus-visible`，不得仅靠颜色表示焦点 |
| MUI-P2-12 | 弹窗焦点管理 | 打开后聚焦、焦点锁定、Esc 关闭、关闭后返回触发按钮 |
| MUI-P2-13 | 语义化可点击元素 | 分类 chip、行操作等使用 `button`/链接，不用只有 `onclick` 的 `span` 或 `tr` |
| MUI-P2-14 | 状态非颜色表达 | 状态同时包含文字/图标；色彩对比达到 WCAG AA 基线 |
| MUI-P2-15 | 动态状态播报 | 保存、测试和后台动作结果使用 `aria-live`，屏幕阅读器可获知变化 |
| MUI-P2-16 | 减少动态效果 | 尊重 `prefers-reduced-motion`，轮询刷新不抢焦点、不重置用户展开状态 |

### 5.3 前端安全与可维护性

| ID | 优化项目 | 说明 |
| --- | --- | --- |
| MUI-P2-17 | 集中请求封装 | 将认证头、超时、JSON 解析、错误处理和 401 处理集中到共享脚本 |
| MUI-P2-18 | 集中 HTML 转义 | 统一 `esc` 实现并减少字符串拼接 `innerHTML`；动态属性和文本分别编码 |
| MUI-P2-19 | 收敛内联脚本 | 将大型页面脚本迁入静态资源，为后续严格 CSP/nonces 做准备 |
| MUI-P2-20 | 内容安全策略 | 在兼容验证后启用 CSP，至少限制脚本、对象、frame 和外部连接来源 |
| MUI-P2-21 | 前端依赖版本管理 | 固定本地 HTMX 等依赖版本，记录许可证和升级验证，不运行 CDN 代码 |
| MUI-P2-22 | 浏览器兼容基线 | 明确便携部署支持的 Edge/Chrome 最低版本并在构建/测试中固定 |

## 6. API 与状态模型建议

为避免页面通过日志文本或多个接口猜测状态，建议扩展 `/api/status`，示意如下：

```json
{
  "observed_at": "2026-08-07T10:30:00+08:00",
  "status_ttl_seconds": 15,
  "process_status": {
    "supervised": true,
    "stale": false,
    "launcher_pid": 1234,
    "processes": [
      {
        "name": "connector",
        "pid": 1235,
        "state": "running",
        "restarts": 1,
        "last_exit_code": null,
        "loaded_config_revision": "sha256:..."
      }
    ]
  },
  "maintenance": {
    "last_attempt_at": "2026-08-07T02:00:00+08:00",
    "last_success_at": "2026-08-07T02:00:05+08:00",
    "integrity": "ok",
    "backup_age_seconds": 30595,
    "free_gb": 48.2,
    "min_free_gb": 2.0,
    "error_code": null
  },
  "readiness": {
    "ready": true,
    "checks": []
  },
  "sources": []
}
```

约束：

- 状态由服务端给出 `observed_at`、TTL 和稳定状态码，浏览器只负责显示。
- 路径、Token、DSN、用户名和 SQL 内容必须脱敏；状态接口不得回显 secrets。
- “未知”必须作为独立状态，不能默认映射为健康。
- 推算值必须带 `derived: true` 或等价标识，不能与实际调度器/执行记录混淆。
- maintenance 状态文件采用临时文件 + 原子替换，读取失败保留明确错误而非空对象。

## 7. 测试项目

| ID | 测试项目 | 最低覆盖 |
| --- | --- | --- |
| MUI-T01 | 状态渲染单元测试 | 运行、重启中、熔断、停止、过期、未知 |
| MUI-T02 | 请求错误测试 | 401、403、409、422、500、非 JSON、超时、断网 |
| MUI-T03 | 配置交互测试 | 对账字段、TLS/重试字段、revision 冲突、自动/人工重启提示 |
| MUI-T04 | 维护状态测试 | 成功、过期、磁盘不足、备份损坏、maintenance 停止 |
| MUI-T05 | generation/推送测试 | 正常完成、心跳失败、旧 generation 拒绝、批次重试、回执命中 |
| MUI-T06 | 动作并发测试 | 双击、请求超时、任务已运行、页面刷新后恢复跟踪 |
| MUI-T07 | 认证流程测试 | 首次登录、错误 Token、Token 过期、退出、重新登录 |
| MUI-T08 | 可访问性测试 | 键盘路径、焦点、标签页、弹窗、颜色对比、动态播报 |
| MUI-T09 | 响应式视觉测试 | 360px、768px、900px、桌面宽度；表格、导航、弹窗截图 |
| MUI-T10 | 真实便携包 E2E | 首配→选表→启动→同步→自动重启→备份→故障恢复 |
| MUI-T11 | 安全回归 | secrets 不回显、HTML 注入文本被转义、CSP 生效后主要流程可用 |
| MUI-T12 | 长期轮询测试 | 页面持续打开、隐藏/恢复、API 短暂中断后恢复，无请求风暴和内存持续增长 |

现有 HTML 测试主要验证页面可返回，新增功能必须补充状态分支和关键交互断言，不能只检查
模板中是否存在某段文本。

## 8. 推荐实施顺序

1. **统一请求与认证错误处理**：先消除“错误显示为空数据”的误导。
2. **真实进程健康与全局横幅**：消费现有 `process_status`，建立可信首页。
3. **维护结果持久化与状态 API**：补齐备份、磁盘和清理可观测性。
4. **对账配置与状态**：补齐已存在后端能力的配置入口和运行结果。
5. **统一重启语义与配置 revision 生效状态**：让保存后的行为可验证。
6. **generation、回执、对账详情**：增强跨机推送排障能力。
7. **高级连接参数、告警模型与诊断摘要**：降低现场维护成本。
8. **响应式、可访问性、CSP 和完整 E2E**：完成质量收口。

## 9. 生产验收门槛

满足以下条件后，中间机管理界面才能作为“无人值守生产运维入口”验收：

- [ ] P0 项目全部完成且有自动化测试。
- [ ] 手工终止 connector 后，15 秒内出现全局故障告警，launcher 自动恢复后告警自动关闭并留下事件记录。
- [ ] 停止 launcher 后，状态明确变为过期/不可监管，不显示正常运行。
- [ ] Token 错误、API 500 和网络中断均不会显示成空数据或健康状态。
- [ ] 每日备份结果、完整性和磁盘余量可见；备份过期或失败会告警。
- [ ] L1/深度对账配置可保存、可验证，页面能区分计划与实际结果。
- [ ] 配置保存后能明确看到自动重启、人工重启或已加载 revision。
- [ ] 便携包真实 E2E 覆盖首次配置、抽取、推送、generation、重启恢复和维护备份。

## 10. 非目标

- 不把中间机管理界面扩展为平台 Vue Console 的替代品。
- 不在浏览器中提供任意 SQL、任意命令执行或 ERP 写回能力。
- 不在页面中回显、下载或复制 Token、密码、完整 DSN 等 secrets。
- 不提供无确认的在线数据库覆盖恢复操作。
- 不因为加入状态页面而改变现有抽取、推送、generation 和水位事务语义。

## 11. 相关文档

- [02 · 抽取框架](02-extraction.md)
- [05 · 控制台与管理界面](05-console.md)
- [06 · 中间机独立抽取表配置](06-middle-table-extraction-config.md)
- [便携包部署](../runbook/portable.md)
- [推送链路验收](../runbook/push-validation.md)
