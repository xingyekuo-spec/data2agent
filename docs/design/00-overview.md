# 00 · 产品总览

> 状态:当前产品设计基线 r2(2026-07-22)· 本文是其余设计文档的入口;版本优先级与发布门槛以[路线图](../roadmap.md)为准。

## 1. 一句话定位

**Data to Agent, for factories** —— 把中小制造企业锁在无 API 老系统里的数据,安全地接给任何 AI Agent。

## 2. 问题与用户

中小制造企业(50–500 人)的经营数据分散在鼎捷 / 金蝶 / 用友 ERP、MES、SRM、CAD 和大量 Excel 里。这些系统普遍没有可用的 API,厂商接口贵且慢;而 Agent 时代的全部价值前提是"数据可达"。

| 用户 | 诉求 | 对应交付 |
| --- | --- | --- |
| 工厂老板 / 厂长 | 30 秒看到 Agent 用自家数据干活(接单评审卡) | 接单评审参考链(docs 04) |
| 工厂 IT / 实施伙伴 | 敢把它装进内网:只读、白名单、可审计 | 抽取框架(docs 02) |
| Agent 开发者 | 五分钟拿到干净的业务对象和指标口径 | MCP 网关(docs 03) |
| 连接器贡献者 | 清晰的模板契约,贡献金蝶 / 用友 binding | 元模型(docs 01) |

## 3. 总体架构

```
源系统(鼎捷 E10 / 易飞 / 金蝶 / 用友 / Excel …)
   │  只读适配器:白名单 / 错峰限时 / 限流 / 全量 SQL 审计     → docs 02
   ▼
原样落地层 raw_*:水位增量 + 回看 + 分段对账 + 隔离区          → docs 02
   ▼
声明式映射:元模型 binding(单一事实来源)                     → docs 01
   ▼
业务对象层(Customer / SalesOrder / …)+ 指标口径(MetricDef)
   ▼
MCP 网关:query_objects / query_metrics,看/说/做 治理档位     → docs 03
   ▼
任何支持 MCP 的 Agent(接单评审参考链 → docs 04)
```

上图是**逻辑架构**。现场**物理部署**(薄中间服务器 + 内网数据平台、内网流式 agent 出站推送)见 docs 02 §12。

核心设计原则(全部已在代码中兑现或将兑现,违反即 bug):

1. **契约先行**:元模型是唯一事实来源;映射引擎、MCP 参数校验、对账工具全部消费它,不允许旁路。抽取表清单在 `connect.yaml` 显式配置,不依赖 binding 自动推导。
2. **诚实声明**:binding 分 `draft/verified`,指标分 `certified/draft/deprecated`;未经现场校准的口径,警示必须贯穿到 Agent 收到的每个响应里。
3. **安全可审计**:碰客户数据库的每一行代码都在本仓库、可审计;只读、白名单、限时、限流是代码强制,不是文档承诺。
4. **按需拉动**:对象模板、指标实现、参考能力都由真实场景拉动,不为完整性提前建设。

## 4. 产品范围(诚实声明)

data2agent 当前已完成 `v0.3.0`:抽取框架、国产 ERP 参考连接器、元模型与模板、
MCP 网关“看/说”档、管理界面、E10-like 参考数据链、Vue Console、数据集原子发布、映射 Preview、
字段血缘、MCP 会话证据和一键验收均在本仓库内,可运行、可审计。
它适合做受控只读影子试运行,但尚未宣告达到真实工厂正式生产试点就绪:
跨机对账(E6b)、批次回执、生产加密传输、凭据治理、SQLite 负载/备份基线
与连续运行验收仍属于 v0.4。

做不做的判断标准是**按需拉动**:能力由真实场景驱动,不为完整性提前建设。
当前版本已覆盖“数据到达 Agent + 接单评审”的参考链、单机验证和关键证据追溯;后续可试点阶段见[路线图](../roadmap.md)。
口径校准(draft→certified)、主数据对齐、“做”档审批治理和行业知识包仍为后续能力。

## 5. MVP 与验收(渔具客户,鼎捷 E10)

首个验证工厂为渔具外销厂,ERP 为鼎捷 E10,核心场景是**接单评审**(询单 → 历史成交 / 毛利 / 交期 → 建议卡)。

验收标准:

1. E10 只读抽取在客户内网稳定运行:错峰窗口内完成日增量,连续一周无事故;
2. 首批 5 个对象(Customer / Material / Quotation / SalesOrder / SalesOrderLine)binding 经现场数据字典核对,全部置为 `verified`;
3. `quote_response_hours` 于启动周完成基线实测(MVP 验收指标);
4. 接单评审卡通过老板测试:非技术人 30 秒看懂 Agent 干了什么。

## 6. 路线与当前状态(2026-07-22)

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| ① 纵向切片 | 元模型 → E10-like 参考库 → 映射引擎首块 → MCP lite | ✅ |
| ② 抽取框架 | 适配器 / 落地 / 增量 / 对账 / 隔离区 / 调度限流(docs 02,E1–E5) | ✅ |
| ③ 参考链完整化 | docker compose 全链路 + 网关「说」档 + 接单评审参考链(docs 04) | ✅ |
| ③′ 现场部署形态 | 便携包(`data2agent.exe`)+ 平台 Vue Console + 中间机轻量管理界面 + 推送 runbook | ✅ |
| ④ v0.2 可观察 | Vue Console + 契约/Mock + 管道/运行/数据/MCP 可视化 | ✅ |
| ⑤ v0.3 可验证 | 字段血缘 + 映射 preview + 数据集版本 + 会话证据 + 一键验收 | ✅ (`v0.3.0`) |
| ⑥ v0.4 可试点 | 批次回执 + E6b + 加密传输 + SQLite 基线 | 待建 |
| ⑦ 现场验证 | 进厂核对字典、binding 置 verified、连续运行并验收 | 未开始(依赖 v0.4 与外部条件) |
| 持续 | 模板 5→18(按需)、金蝶 / 用友 binding(社区)、达到阈值后评估 PG | 拉动式 |

## 7. 文档索引

- [01 元模型与模板包](01-metamodel.md) —— 已实现,契约定义
- [02 抽取框架](02-extraction.md) —— 抽取与拆机部署详设
- [03 MCP 网关](03-mcp-gateway.md) —— lite 现状 + 治理档位演进
- [04 参考数据链](04-reference-chain.md) —— 测试资产、拓扑与参考链
- [05 运维控制台](05-console.md) —— 平台 Vue Console 与中间机管理入口
- [07 呆滞库存 MCP](07-dead-stock-mcp.md) —— 呆滞库存对象、指标、确定性口径与验收边界
- [路线图](../roadmap.md) —— 当前 v0.3.0 状态与 v0.4 可试点计划
- 现场 runbook:[便携包](../runbook/portable.md) · [推送验收](../runbook/push-validation.md)
- [E10 参考表字典](../dict/digiwin_e10.md) —— 生成文件,勿手改
- 接单评审参考链:`python -m data2agent.showroom.review_demo`
